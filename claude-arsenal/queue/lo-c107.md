# T18: Acceptance close-out — reference documents, README gallery, determinism and zero-dependency checks

Plan row: `T18` in `status/plan.md`. Size: M.
Specification: `status/specification.md`.

## Acceptance gate

`nondeterministic_reruns == 0`

Every fixture renders byte-identically on a second run. The package imports and renders with PATH emptied of non-Python executables, proving system_dependencies == 0. The three reference documents render without FitError or LayoutError — the brief's second acceptance test.

```bash
uv run pytest tests/test_acceptance.py -q
uv run pytest -q
```

## Tests

Write these RED first, before any production code.

`test_every_fixture_renders_identically_on_a_second_run`.
`test_package_renders_with_no_system_binaries_available`.
`test_three_reference_documents_render_without_fit_or_layout_errors`.

## Location

`tests/test_acceptance.py`, `README.md`, `examples/`
