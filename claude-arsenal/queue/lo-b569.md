# T11: Graph kinds — flow, tree, cycle

Plan row: `T11` in `status/plan.md`. Size: L.
Specification: `status/specification.md`.

## Acceptance gate

`text_overflow_count == 0`

A flow document round-trips to safe SVG with no text overflow. A cycle's arrows all follow one direction, so a cycle reads as a cycle. Tree ranks increase with depth, so hierarchy is visible in the geometry.

```bash
uv run pytest tests/test_kinds_graph.py -q
uv run ruff check .
uv run mypy .
```

## Tests

Write these RED first, before any production code.

`test_flow_document_renders_svg_passing_inline_safety`.
`test_cycle_arrows_all_follow_one_direction`.
`test_tree_child_ranks_increase_with_depth`.

## Location

`src/drawspec/kinds/graph.py`, `tests/test_kinds_graph.py`
