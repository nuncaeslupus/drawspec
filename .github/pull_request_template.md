<!--
  A short description is fine. The three prompts below are the ones that are
  easy to forget and annoying to catch in review.
-->

## What this changes



## Checks

- [ ] `make lint` and `make test` pass
- [ ] If a field, kind or theme key moved: `make docs && make schema`, and the
      regenerated files are in this diff
- [ ] If the drawings moved: `make gallery`, and the regenerated gallery is in
      this diff — the picture diff is the review
