---
id: lo-e854
title: "G2: chart marks — bars, then area and stacked"
priority: 5
status: done
pr: https://github.com/nuncaeslupus/drawspec/pull/24
---

The standing request. Decide first whether to keep hand-rolling the chart or
wrap an established library, and record the decision with its reasons.

## Acceptance gate
A chart document can ask for bars and get them, on the same axes, ticks and
gutter arithmetic the line marks already use. Greyscale-safe. `make lint` and
`make test` clean.

```bash
uv run pytest tests/test_kinds_chart.py -q && uv run ruff check . && uv run mypy .
```

## Location
`src/drawspec/charts.py`, `src/drawspec/schema.py`.
