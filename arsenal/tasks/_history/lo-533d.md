---
id: lo-533d
title: "T5: Line breaking and text block sizing"
priority: 5
deps: [lo-4f9b, lo-b94c]
tags: [drawspec]
status: done
pr: https://github.com/nuncaeslupus/drawspec/pull/2
---

Plan row: `T5` in `status/plan.md`. Size: M.
Specification: `status/specification.md`.

## Acceptance gate

`text_overflow_count == 0`

Every line returned by the wrapper measures at or below the given max width, and block height is derived from line count times the theme's line-height rather than estimated. An unbreakable run wider than the box raises FitError rather than overflowing.

```bash
uv run pytest tests/test_wrap.py -q
uv run ruff check .
uv run mypy .
```

## Tests

Write these RED first, before any production code.

`test_wrap_produces_lines_all_within_the_given_width` — no line exceeds the width.
`test_wrap_word_longer_than_max_width_raises_fiterror` — fails loudly.
`test_wrap_block_height_equals_lines_times_line_height` — height is derived.

## Location

`src/drawspec/text/wrap.py`, `tests/test_wrap.py`
