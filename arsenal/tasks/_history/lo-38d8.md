---
id: lo-38d8
title: "T15: CLI — render, validate, theme check, schema, with width/height/profile overrides"
priority: 5
deps: [lo-b569, lo-2f7f, lo-f75a, lo-0e20]
tags: [drawspec]
status: done
pr: https://github.com/nuncaeslupus/drawspec/pull/15
---

Plan row: `T15` in `status/plan.md`. Size: M.
Specification: `status/specification.md`.

## Acceptance gate

`cli_exit_code_mismatches == 0`

Every command exits with the code documented in specification.md section 5.4. Validation failures print a JSON pointer so the violation is locatable the way an HTML validator names a line and an attribute.

```bash
uv run pytest tests/test_cli.py -q
uv run ruff check .
uv run mypy .
```

## Tests

Write these RED first, before any production code.

`test_render_valid_document_writes_svg_and_exits_zero`.
`test_validate_invalid_document_prints_json_pointer_and_exits_one`.
`test_theme_check_ambiguous_theme_exits_one`.
`test_width_flag_overrides_the_document_width`.
`test_profile_flag_selects_the_embedding_profile`.

## Location

`src/drawspec/cli.py`, `tests/test_cli.py`. Contract: specification.md section 5.4.
