---
id: lo-4c51
title: "T7: SPIKE — implement LayoutEngine against grandalf and a direct layered implementation, compare, decide"
priority: 5
deps: [lo-b7a8]
tags: [drawspec]
status: done
pr: https://github.com/nuncaeslupus/drawspec/pull/3
---

Plan row: `T7` in `status/plan.md`. Size: M.
Specification: `status/specification.md`.

## Acceptance gate

`candidate_engines_rendered == 2`

Both candidate engines return valid positions, with no overlapping node boxes, for all three reference documents. The deliverable is a decision recorded in the plan with its escape route, backed by rendered output — pictures, not argument. Note the licence position: grandalf is dual GPLv2/EPL-1.0, and only the EPL-1.0 arm is compatible with shipping MIT code around it.

```bash
uv run pytest tests/test_layout_spike.py -q
```

## Tests

Write these RED first, before any production code.

`test_spike_each_candidate_engine_lays_out_all_reference_documents` — both engines return valid positions for all three reference documents with no overlapping boxes.

## Location

`src/drawspec/layout/`, `tests/test_layout_spike.py`. This task exists to settle the open question in specification.md section 4 BEFORE T8 implements anything.
