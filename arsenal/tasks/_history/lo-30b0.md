---
id: lo-30b0
title: "T17: Embedding targets — both profiles verified into Markdown, HTML and PDF"
priority: 5
deps: [lo-38d8]
tags: [drawspec]
status: done
pr: https://github.com/nuncaeslupus/drawspec/pull/17
---

Plan row: `T17` in `status/plan.md`. Size: M.
Specification: `status/specification.md`.

## Acceptance gate

`embedding_safety_violations == 0`

Every fixture renders in both profiles and passes the safety validator, measured PER PROFILE rather than aggregated — the less-exercised standalone path is exactly where a defect hides. Two fixtures on one HTML page collide on neither id nor selector. A standalone fixture converts to PDF with no unresolved currentColor, so the ink is the theme's rather than a converter default. Greyscale conversion preserves every role distinction.

```bash
uv run pytest tests/test_embedding.py -q
```

## Tests

Write these RED first, before any production code.

`test_every_fixture_passes_safety_in_both_profiles`.
`test_two_fixtures_in_one_html_page_have_no_id_or_selector_collision`.
`test_standalone_fixture_converts_to_pdf_without_unresolved_colour`.
`test_greyscale_conversion_preserves_role_distinctions`.

## Location

`tests/test_embedding.py`, `src/drawspec/emit.py`. Contract: specification.md section 1, Output targets.
