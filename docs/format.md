<!-- Generated by tools/docs.py from the code it describes. Run `make docs`; do not edit by hand. -->

# The document format

A document is JSON. It says what the diagram **means** — the nodes, the
edges, the words, and a semantic role for each — and it carries no
coordinate, no size and no colour, because those are the tool's output
and never its input. The [refused fields](#what-may-not-be-written) at
the end of this page are that rule made mechanical.

Every document declares `"version": 1` and one of the
13 kinds. The machine-readable form of this page is the published
JSON Schema, `https://drawspec.dev/schema/drawspec-v1.schema.json` — point an editor at it with a `$schema` key and
get completion and inline validation for free.

```json
{ "$schema": "https://drawspec.dev/schema/drawspec-v1.schema.json", "version": 1, "kind": "flow", "nodes": [] }
```

`drawspec validate doc.json` prints every violation with the JSON pointer of
the place it was written, so one pass over its output is one edit.

## Every document

These fields are legal whatever the kind is.

| Field | Type | Required | What it is |
|---|---|---|---|
| `$schema` | string | no | Optional, and only for editors: the URL of this schema. |
| `version` | integer | **yes** | 'version' is required. Declare it so a future format change is a loud failure rather than a misread document. |
| `kind` | string — one of `flow`, `tree`, `cycle`, `stack`, `timeline`, `columns`, `matrix`, `pyramid`, `rings`, `funnel`, `chart`, `quadrant`, `curve` | **yes** | Which of the thirteen diagrams this is. It selects the fields that are legal below, so it is the first thing to get right. |
| `title` | string | no | The diagram's accessible name. |
| `description` | string | no | The diagram's accessible description. |
| `width` | number, greater than 0 | no | Overrides the theme's canvas width. Binding. |
| `height` | number, greater than 0 | no | Advisory unless height_binding is true. |
| `height_binding` | boolean | no | Makes height a constraint rather than a hint. |
| `theme` | string | no | A bundled theme name or the path of a theme file. |

## What each kind adds

The `kind` selects a family, and the family selects which further fields are
legal. A field belonging to another family is refused by name rather than
ignored — a `chart` with `nodes` in it is a mistake worth hearing about.

### `flow`, `tree`, `cycle`

| Field | Type | Required | What it is |
|---|---|---|---|
| `nodes` | array of [node](#node-object), at least 1 | **yes** | The boxes. Order is not position — the layout decides that. |
| `edges` | array of [edge](#edge-object) | no | What connects to what. A `tree` takes one edge per child. |
| `groups` | array of [group](#group-object) | no | Boxes drawn around sets of nodes, each with its own caption. |

### `stack`, `timeline`, `columns`

| Field | Type | Required | What it is |
|---|---|---|---|
| `items` | array of [item](#item-object), at least 1 | **yes** | The entries, in the order they are meant to be read: down for `stack`, along for `timeline` and `columns`. |

### `matrix`

| Field | Type | Required | What it is |
|---|---|---|---|
| `cells` | array of [cell](#cell-object), at least 1 | **yes** | What is in the grid. A cell states where it starts, not where it is drawn. |
| `columns` | array of string | no | Column headings, left to right. Omit for a matrix with none. |
| `rows` | array of string | no | Row headings, top to bottom. |

### `pyramid`

| Field | Type | Required | What it is |
|---|---|---|---|
| `levels` | array of [level](#level-object), at least 1 | **yes** | Apex first. The widest band is the last one written. |

### `rings`

| Field | Type | Required | What it is |
|---|---|---|---|
| `rings` | array of [ring](#ring-object), at least 1 | **yes** | Innermost first, working outwards. |

### `funnel`

| Field | Type | Required | What it is |
|---|---|---|---|
| `stages` | array of [stage](#stage-object), at least 1 | **yes** | Widest first: a funnel is a pyramid lying down. |

### `chart`

| Field | Type | Required | What it is |
|---|---|---|---|
| `axes` | [axes object](#axes-object) | **yes** | Both axes. An unlabelled axis cannot be read, so both are required. |
| `series` | array of [series](#series-object), at least 1 | **yes** | What is plotted. More than one gets a legend. |
| `values` | boolean | no | Whether to write each point's own number on the drawing. Defaults to true; set false for a chart whose shape is the message and whose numbers would be noise. |

### `quadrant`

| Field | Type | Required | What it is |
|---|---|---|---|
| `axes` | [axes object](#axes-object) | **yes** | The two named axes whose crossing makes the four quadrants. |
| `positions` | array of [position](#position-object), at least 1 | **yes** | The items placed in the plane, each by what it scores, not by where it goes. |

### `curve`

| Field | Type | Required | What it is |
|---|---|---|---|
| `axes` | [axes object](#axes-object) | **yes** | Both axes. A named shape still needs saying what it is a shape of. |
| `curves` | array of [curve](#curve-object), at least 1 | **yes** | The shapes drawn, each through its own waypoints. |

## The objects

What the arrays above hold. Every one of them refuses a field it does not
name, so a misspelt key is an error at the point it was written.

### `node` object

| Field | Type | Required | What it is |
|---|---|---|---|
| `id` | string | **yes** | Unique within the document. |
| `text` | string | **yes** | The words themselves. May carry inline spans — `code` for the monospace role and **bold** for emphasis — because those are semantic, not typographic. |
| `role` | string — one of `start`, `step`, `decision`, `terminal`, `emphasis`, `note`, `group` | no | The semantic role, which the theme resolves to an appearance. Defaults to 'step'. |
| `note` | string | no | A short aside attached to this node. |

### `edge` object

| Field | Type | Required | What it is |
|---|---|---|---|
| `from` | string | **yes** | The id of the source node. |
| `to` | string | **yes** | The id of the target node. |
| `label` | string | no | A short label placed along the edge. |
| `role` | string — one of `flow`, `link`, `exchange`, `weak`, `owns`, `strong` | no | The semantic role, which the theme resolves to an appearance. Defaults to 'flow'. |

### `group` object

| Field | Type | Required | What it is |
|---|---|---|---|
| `id` | string | **yes** | Unique within the document. |
| `text` | string | no | The words themselves. May carry inline spans — `code` for the monospace role and **bold** for emphasis — because those are semantic, not typographic. |
| `members` | array of string, at least 1 | **yes** | The ids of the nodes this group contains. |

### `item` object

| Field | Type | Required | What it is |
|---|---|---|---|
| `id` | string | no | Optional; generated from position when absent. |
| `text` | string | **yes** | The words themselves. May carry inline spans — `code` for the monospace role and **bold** for emphasis — because those are semantic, not typographic. |
| `role` | string — one of `start`, `step`, `decision`, `terminal`, `emphasis`, `note`, `group` | no | The semantic role, which the theme resolves to an appearance. Defaults to 'step'. |
| `note` | string | no | A short aside attached to this entry. |
| `at` | number | no | For `timeline`: when this happened, so the gaps mean something. Give it to every entry or to none. |

### `level` object

| Field | Type | Required | What it is |
|---|---|---|---|
| `text` | string | **yes** | The words themselves. May carry inline spans — `code` for the monospace role and **bold** for emphasis — because those are semantic, not typographic. |
| `role` | string — one of `start`, `step`, `decision`, `terminal`, `emphasis`, `note`, `group` | no | The semantic role, which the theme resolves to an appearance. Defaults to 'step'. |
| `note` | string | no | A short aside drawn beside this level. |

### `ring` object

| Field | Type | Required | What it is |
|---|---|---|---|
| `text` | string | **yes** | The words themselves. May carry inline spans — `code` for the monospace role and **bold** for emphasis — because those are semantic, not typographic. |
| `role` | string — one of `start`, `step`, `decision`, `terminal`, `emphasis`, `note`, `group` | no | The semantic role, which the theme resolves to an appearance. Defaults to 'step'. |

### `cell` object

| Field | Type | Required | What it is |
|---|---|---|---|
| `text` | string | **yes** | The words themselves. May carry inline spans — `code` for the monospace role and **bold** for emphasis — because those are semantic, not typographic. |
| `column` | integer | **yes** | Which column this cell starts in, counting from zero. |
| `row` | integer | **yes** | Which row it starts in, from zero. |
| `across` | integer | no | How many columns it covers. Default 1. |
| `down` | integer | no | How many rows it covers. Default 1. |
| `group` | string | no | Which group this cell belongs to. Cells sharing a group are filled alike; the theme decides how. Naming the fill itself is not an author's decision. |
| `role` | string — one of `start`, `step`, `decision`, `terminal`, `emphasis`, `note`, `group` | no | The semantic role, which the theme resolves to an appearance. Defaults to 'step'. |

### `waypoint` object

| Field | Type | Required | What it is |
|---|---|---|---|
| `text` | string | no | What to call this point. Omit for a point that only shapes the curve. |
| `across` | number | **yes** | Where this sits on the horizontal axis. Data, not a coordinate. |
| `up` | number | **yes** | Where this sits on the vertical axis. Data, not a coordinate. |

### `curve` object

| Field | Type | Required | What it is |
|---|---|---|---|
| `name` | string | no | Drawn where the curve ends. Omit it when there is only one curve and the axes already say what it is. |
| `waypoints` | array of [waypoint](#waypoint-object), at least 2 | **yes** | The points the curve passes through, in order. Two is a line. |
| `role` | string — one of `start`, `step`, `decision`, `terminal`, `emphasis`, `note`, `group` | no | The semantic role, which the theme resolves to an appearance. Defaults to 'step'. |

### `position` object

| Field | Type | Required | What it is |
|---|---|---|---|
| `text` | string | **yes** | The words themselves. May carry inline spans — `code` for the monospace role and **bold** for emphasis — because those are semantic, not typographic. |
| `across` | number | **yes** | Where this sits on the horizontal axis. Data, not a coordinate. |
| `up` | number | **yes** | Where this sits on the vertical axis. Data, not a coordinate. |
| `role` | string — one of `start`, `step`, `decision`, `terminal`, `emphasis`, `note`, `group` | no | The semantic role, which the theme resolves to an appearance. Defaults to 'step'. |

### `stage` object

| Field | Type | Required | What it is |
|---|---|---|---|
| `text` | string | **yes** | The words themselves. May carry inline spans — `code` for the monospace role and **bold** for emphasis — because those are semantic, not typographic. |
| `role` | string — one of `start`, `step`, `decision`, `terminal`, `emphasis`, `note`, `group` | no | The semantic role, which the theme resolves to an appearance. Defaults to 'step'. |
| `note` | string | no | A short aside drawn beside this stage. |

### `axis` object

| Field | Type | Required | What it is |
|---|---|---|---|
| `label` | string | **yes** | Required: an unlabelled axis cannot be read. |
| `unit` | string | no | What the numbers are in, written after the label. |
| `min` | number | no | Where the axis starts. Omit to take it from the data. |
| `max` | number | no | Where the axis ends. Omit to take it from the data. |
| `categories` | array of string | no | Names for the positions 1, 2, 3 … along this axis, instead of numbers. What a bar chart's horizontal axis usually is: the quarters, the service models, the teams. |

### `axes` object

| Field | Type | Required | What it is |
|---|---|---|---|
| `horizontal` | [axis object](#axis-object) | **yes** | The axis across. |
| `vertical` | [axis object](#axis-object) | **yes** | The axis up. |

### `series` object

| Field | Type | Required | What it is |
|---|---|---|---|
| `name` | string | **yes** | What to call this series, in the legend and at its end. |
| `data` | array of `[x, y]` pairs, at least 1 | **yes** | The values to plot, as [x, y] pairs. Data, not coordinates. |
| `role` | string — one of `start`, `step`, `decision`, `terminal`, `emphasis`, `note`, `group` | no | The semantic role, which the theme resolves to an appearance. Defaults to 'step'. |
| `mark` | string — one of `line`, `bar`, `area` | no | How this series is drawn: a line, bars, or a filled area. Default 'line'. |
| `stack` | string | no | Series sharing a stack name are piled on each other rather than drawn side by side. Bars and areas only. |

## What may not be written

These field names are refused wherever they appear, each with the reason
below and the JSON pointer of the place it was written. This is the list that
makes the failures in `docs/brief.md` unexpressible rather than merely
discouraged: a document cannot say where a box goes, so a box cannot go in the
wrong place.

| Field | Why not |
|---|---|
| `anchor` | edge geometry comes from the edge's semantic role |
| `arrow_head` | say role: "link", not arrow_head: "none" — head geometry is the theme's |
| `canvas` | derived from width and the content |
| `color` | appearance is a property of the role, not of the element |
| `cx` | coordinates are the tool's output, never its input |
| `cy` | coordinates are the tool's output, never its input |
| `d` | coordinates are the tool's output, never its input |
| `dash` | line treatment comes from the edge's semantic role |
| `dx` | coordinates are the tool's output, never its input |
| `dy` | coordinates are the tool's output, never its input |
| `fill` | appearance is a property of the role, not of the element |
| `font_family` | type is selected by semantic role from the theme's scale |
| `font_size` | type is selected by semantic role from the theme's scale |
| `font_weight` | type is selected by semantic role from the theme's scale |
| `height` | box geometry is derived from measured text plus theme padding |
| `layer` | overlap is resolved by the layout, not declared |
| `order` | overlap is resolved by the layout, not declared |
| `points` | coordinates are the tool's output, never its input |
| `port` | edge geometry comes from the edge's semantic role |
| `stroke` | appearance is a property of the role, not of the element |
| `stroke_width` | appearance is a property of the role, not of the element |
| `viewBox` | derived from width and the content |
| `width` | box geometry is derived from measured text plus theme padding |
| `x` | coordinates are the tool's output, never its input |
| `y` | coordinates are the tool's output, never its input |
| `z` | overlap is resolved by the layout, not declared |

